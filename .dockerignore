# Keep the Docker build context small: the build_utils/Dockerfile only needs
# Cargo.toml + Cargo.lock (the source tree is bind-mounted at run time, not
# copied into the image). Excluding target/ especially avoids shipping multiple
# GB of build artifacts as context.
target/
.git/
examples/
cpp/
*.tar
*.tar.gz
